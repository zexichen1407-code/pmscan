# 套利做市机器人 — 实施计划（基于 0xp3nny 逆向结果）

> 复刻对象：同市场双边挂单凑对合并（Strategy B）为地基，neg-risk 转换（Strategy A）为后期。
> 报价规则来自 `QUOTING_ALGO.md`；风险认知来自 `STOPLOSS.md` / `TRUE_PNL.md`。

## 0. 生死线（先认清，再动手）
- **边际真实只有 ~2–3%，且在压缩**——连 0xp3nny 自己近 7 天都在浮亏。你进的是一个**正在变薄、已经拥挤**的赛道。
- 这门生意靠三样撑着：①高频机器人执行 ②maker 0 手续费 ③资金高速周转（他 $5 万本金循环 160 倍）。缺任何一样，薄边际就会被吃光。
- **2026 年 4 月起有 taker 费**：你追腿吃单要付费，必须把它算进边际下限，否则名义赚、实际亏。
- **最高杠杆动作 = Phase 0–1 用近零成本先验证"这点边际在你的真实成交率下活不活得了"，再决定投钱投基建。** 这一步省下后面所有风险。

## 1. 范围
| 策略 | 做不做 | 原因 |
|---|---|---|
| **B 同市场凑对合并**（in-play 二元体育） | ✅ 地基，先做 | 零结算风险、数学可控、单人可做 |
| **A neg-risk 转换** | ⏳ 后期 | 主引擎但有残腿风险、N 腿风控复杂 |
| C 荷兰赌/赌赛果 | ❌ | 含方向性，不是纯套利 |

## 2. 系统架构（7 模块，我写代码）
```
[1 行情]  CLOB websocket 实时订阅每条腿 best bid/ask
   ↓
[2 报价引擎]  实现 QUOTING_ALGO.md：钉 bid_yes+bid_no ≤ C、贴中价下方、库存顺势、波动收紧
   ↓
[3 下单]  py-clob-client 签 EIP-712，双边挂被动限价单(maker)
   ↓
[4 成交监听]  websocket 回报 → 维护每市场 YES/NO 库存 + 逐笔成本
   ↓
[5 风控]  ★ 残腿上限 / 追腿禁令(不补到 p+q>1) / 临盘撤单 / 价格地板顶 / 日亏熔断
   ↓
[6 结算工人]  web3.py 批量 mergePositions（攒够 ~170 对再合并省 gas）
+ [监控] PnL/库存/熔断推送；顺带跑 0xp3nny 钱包当"赛道拥挤度"信号
```

## 3. 分阶段 + 硬验收闸门（过不了不进下一阶段）
- **Phase 0 — 信号+报价模拟器**（只读，0 资金，3–5 天）
  实时扫盘口算 p+q；回测能否在历史上认出 `QUOTING_ALGO.md` 里那些套利时刻。
  ✅闸门：历史样本里重现出我们手算的错价时刻。
- **Phase 1 — 观察/纸面模式**（live 不下真单，2–4 周）★最重要★
  记录盘口 + 模拟挂单成交，度量**扣费后真实净边际**和**单腿成交率**（一条腿成、另一条没成的频率＝你的方向性暴露）。
  ✅闸门：**纸面净边际 > 0 且单腿暴露可控**。过不了就停——省下后面全部投入。
- **Phase 2 — 微资金 taker**（$几百–2k，1–2 周）
  只做明显错位的吃单两腿+合并，验证"纸面 vs 现实"的缺口。
  ✅闸门：真实成交≈纸面模型，扣 gas + taker 费后为正。
- **Phase 3 — maker 模式 + 全风控 + merge 结算**（小资金，2–4 周）
  切到他真正用的被动挂单 + 模块 5 全套风控。
  ✅闸门：无方向性爆仓、单腿超时处理生效、含返佣净为正。
- **Phase 4 — 扩资金 + Strategy A**（neg-risk 转换 + 残腿风控）

## 4. 技术栈 & 基建
- Python：`py-clob-client`（下单/签名）+ `web3.py`（链上 merge/convert）+ `websockets`
- 本地 SQLite/Parquet 存盘口快照、成交、PnL（便于审计回放）
- 一台云 VM（选 Polymarket 后端同区域降延迟；分钟级套利非微秒级，普通 VPS 即可）
- 复用现有 `pmscan/` 端点知识

## 5. 我做的 vs 你出凭证的
| 我全包 | 只能你来（凭证/钱） |
|---|---|
| 全部 7 模块代码 + 回测 + 纸面框架 + 风控 + 监控 | 生成一个你自己掌握私钥的 Polygon 钱包 |
| 部署脚本、配置、私有仓库 | 充 USDC + 付 gas 的 MATIC |
| 链上授权脚本（approve + setApprovalForAll） | 在 Polymarket 开 CLOB API key/secret/passphrase |
| 0xp3nny 拥挤度监控器 | 开通 VM；**签**那笔授权交易 |

## 6. 起步参数（直接来自逆向，第一版照填）
| 参数 | 起步值 | 备注 |
|---|---|---|
| 目标成对成本 C | 0.97 | 两腿买价之和上限 |
| 边际下限 EDGE_FLOOR | 0.015 **+ 实际手续费** | ⚠️ 必须含 taker 费，否则净亏 |
| 贴盘口 PEG_OFFSET | 低于中价 0.008 | |
| 每笔 CLIP | 25 股 | 两腿一致，不给缺货侧加大 |
| 库存倾斜 | 顺势加便宜腿（非回中） | |
| 单边残腿上限 MAX_OVERHANG | 小（你能扛多少方向性风险） | **最大单笔亏损来源** |
| 重报 REQUOTE | 12 秒 / 漂移 0.005 立刻跟 | |
| 合并批量 | ~170 对/次 | |
| 临盘撤单 | 结算前 180 秒 | 临盘逆选择最毒 |

## 7. 诚实资源账本（约束当事实，不劝退）
- **钱**：几千刀够 Phase 0–2 验证；有意义收入需 ~$5 万+ 周转浮存（不是亏损风险，是反复循环的本金）。
- **gas**：每月上万笔小额 Polygon 交易，计入净 PnL。
- **时间**：观察模式 bot 约 2–3 周；带风控实盘系统数月；之后持续运维。
- **技能**：数学你有；缺口在**市场微观结构 + 风控工程**——这是学习曲线，也是真正的护城河。
- **赛道现实**：边际在压缩、竞争已拥挤、连 incumbent 都在回撤。你的活法＝吃大 bot 懒得碰的长尾小市场/小 size，或接受分一杯羹。别期待肥利。
- **残腿铁律**：套利配对本身安全；危险全在配不上的单边货。他历史上残腿净值≈0 是**几千笔大数定律**的结果——你 N 小的时候大数定律还没生效，单边货能让你单周巨亏。`MAX_OVERHANG` 是命门。

## 8. 建议第一步
直接从 **Phase 0** 开始：我搭信号+报价模拟器 + 对历史套利时刻的回测。零资金、零风险，跑通了才知道值不值得往下走。
